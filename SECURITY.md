# セキュリティガイドライン

このドキュメントでは、FCM Web Push通知を実装する際のセキュリティのベストプラクティスについて説明します。

## 環境変数の取り扱い

### 重要な環境変数
- `NEXT_PUBLIC_FIREBASE_API_KEY`: Firebaseプロジェクトの認証に使用
- `NEXT_PUBLIC_FIREBASE_WEBPUSH_KEY`: Web Push通知のVAPIDキー

### セキュリティ上の注意点
1. **環境変数の管理**
   - 本番環境の環境変数は安全な方法で管理してください
   - `.env.local`ファイルをGitリポジトリにコミットしないでください
   - CI/CDパイプラインでは、シークレット管理サービスを使用してください

2. **VAPID キーの取り扱い**
   - VAPIDキーは必ずFirebase Consoleから取得してください
   - 自己生成したキーは使用しないでください
   - キーの漏洩に注意してください

## Service Worker のセキュリティ

### スコープの設定
- Service Workerのスコープは必要最小限に設定してください
- デフォルトのスコープ設定は、アプリケーションの構成に応じて変更される可能性があります
- スコープを変更する場合は、アプリケーションの要件に応じて適切に設定してください

### 更新戦略
- `updateViaCache: 'none'`を設定し、常に最新のService Workerを使用します
- キャッシュされたバージョンは使用しません

## トークンの取り扱い

### FCMトークンのセキュリティ
1. **トークンの保存**
   - FCMトークンをローカルストレージに保存する場合は暗号化してください
   - トークンの有効期限を適切に管理してください
   - 不要になったトークンは速やかに削除してください

2. **トークンの使用**
   - トークンはサーバーサイドでのみ使用してください
   - クライアントサイドでトークンを表示する場合は、必要最小限の時間のみとしてください
   - トークンをログに出力しないでください

## 通知のセキュリティ

### ペイロードの取り扱い
1. **機密情報**
   - 通知のペイロードに機密情報を含めないでください
   - ユーザー識別子や個人情報は含めないでください
   - 必要な場合は、通知をタップした後にセキュアな通信で取得してください

2. **ペイロードの検証**
   - 受信したペイロードは必ず検証してください
   - 不正なペイロードは破棄してください
   - XSS攻撃を防ぐため、ペイロードのサニタイズを行ってください

## 実装のベストプラクティス

### エラーハンドリング
- すべてのエラーを適切に処理してください
- エラーメッセージにセキュリティ上の機密情報を含めないでください
- デバッグモードでのみ詳細なエラー情報を表示してください

### 権限の管理
- 通知の許可は必要なタイミングでのみ要求してください
- ユーザーが通知を拒否した場合は、適切に対応してください
- 通知の許可状態を定期的に確認してください

## セキュリティ監査

### 定期的なチェック
1. **依存関係の更新**
   - 定期的に依存パッケージを更新してください
   - セキュリティ脆弱性の警告に注意してください
   - 更新後は十分なテストを行ってください

2. **コードレビュー**
   - セキュリティの観点からコードレビューを行ってください
   - 特に以下の点に注意してください：
     - 環境変数の使用
     - トークンの取り扱い
     - エラーハンドリング
     - 入力値の検証

## インシデント対応

### セキュリティインシデントが発生した場合
1. 即座にトークンを無効化してください
2. 影響を受けるユーザーに通知してください
3. 原因を特定し、対策を講じてください
4. 再発防止策を実装してください

## 参考リソース

- [Firebase Security Rules](https://firebase.google.com/docs/rules)
- [Web Push Security](https://developers.google.com/web/fundamentals/push-notifications/web-push-protocol)
- [OWASP Security Cheat Sheet](https://cheatsheetseries.owasp.org/)
